apiVersion: 1
datasources:
  - name: SQLite
    type: frser-sqlite-datasource
    access: proxy
    url: /var/lib/grafana/relay.db
    isDefault: true
    jsonData:
      path: /var/lib/grafana/relay.db
